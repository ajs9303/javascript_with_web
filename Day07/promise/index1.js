/*
const apple = () =>
  new Promise((success, fail) => {
    success("사과");
  });

const iphone = (color, version) =>
  new Promise((success, fail) => {
    success(`${color}색 아이폰 ${version}`);
  });

iphone("초록", "16").then((v) => console.log(v));
*/
