/*
const a = new Promise((success, fail) => {
  const num = +prompt("숫자 입력");
  if (num % 2 == 1) success("쿠키");
  else fail("케이크");
});

a.then((v) => console.log(`${v} 꿀맛!`)).catch((v) => console.log(`${v} 노맛`));
*/
